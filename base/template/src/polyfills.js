/**
 * Should only be used for importing polyfills.
 *
 * This file will bundled separately from the main application
 * and the bundle will only execute in `nomodule` browsers such as IE11.
 *
 * NOTE: polyfills for modern browsers should be included in `index.ts` instead
 * NOTE 2: language feature polyfills should be added via `babel` and/or `typescript`
 */
